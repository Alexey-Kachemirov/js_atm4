'use strict';

class calculator {
    addUs2(a, b) {
        return a + b;
    }

    addUs3(a, b, c) {
        return a + b + c;
    }

    multiplyUs2(a, b) {
        return a * b;
    }

    multiplyUs3(a, b, c) {
        return a * b * c;
    }
}

module.exports = calculator;